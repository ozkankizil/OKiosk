<Window x:Class="OKiosk.PasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OKiosk"
        mc:Ignorable="d"
        Title="Yönetici Girişi" Height="180" Width="320"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="ToolWindow"
        FocusManager.FocusedElement="{Binding ElementName=PasswordBox}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Yönetici Şifresi:" Margin="0,0,0,8"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Height="28" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button IsCancel="True" Content="İptal" Width="80" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button IsDefault="True" Content="Giriş" Width="80" Click="Login_Click"/>
        </StackPanel>
    </Grid>
</Window>
