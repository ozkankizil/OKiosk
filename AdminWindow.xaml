<Window x:Class="OKiosk.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Admin Paneli"
        Height="700"
        Width="700"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20" Orientation="Vertical" VerticalAlignment="Top" >

            <!-- AÇILIŞ SAYFASI -->
            <TextBlock Text="Açılış Sayfası (HomeUrl):" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtHomeUrl"
                     Height="30"
                     Margin="0,0,0,15"/>

            <!-- JAVASCRIPT ALANI -->
            <TextBlock Text="Sayfa Yüklendiğinde Çalışacak JS Kodu:"
                       FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtCustomJs"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     Height="120"
                     Margin="0,0,0,20"/>

            <!-- CSS ALANI -->
            <TextBlock Text="Sayfa Yüklendiğinde Çalışacak CSS Kodu:"
                       FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtCustomCss"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     Height="120"
                     Margin="0,0,0,20"/>

            <!-- ALLOWED ORIGINS ALANI -->
            <TextBlock Text="İzin Verilen Siteler (her satıra bir tane yaz):"
                       FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtAllowedOrigins"
                     AcceptsReturn="True"
                     TextWrapping="NoWrap"
                     VerticalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     Height="120"
                     Margin="0,0,0,25"
                     ToolTip="Örnek:&#10;https://www.google.com&#10;*.google.com&#10;https://www.antalyabb.gov.tr"
                     />

            <!-- BUTON -->
            <Button Content="Değişiklikleri Uygula"
                    Width="200"
                    Height="40"
                    HorizontalAlignment="Right"
                    Click="Apply_Click"/>
        </StackPanel>
    </ScrollViewer>
</Window>
